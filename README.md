# Adoption API

API backend desarrollada con Node.js + Express + Docker.

---

## 🚀 Ejecutar con Docker

```bash
docker pull gottich/adoption-api:latest
docker run -p 3000:3000 gottich/adoption-api:latest

https://hub.docker.com/r/gottich/adoption-api